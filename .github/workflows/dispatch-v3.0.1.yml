name: Dispatch v3.0.1 Release

on:
  pull_request:
    branches:
      - main
    paths:
      - release-trigger-v3.0.1.txt

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    if: github.head_ref == 'release/trigger-v3.0.1'
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch the validated release workflow
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run release.yml --ref main -f tag=v3.0.1 -f action=prepare-and-release
